apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "csi-wekafsplugin.fullname" . }}-test-connection"
  labels:
    {{- include "csi-wekafsplugin.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "csi-wekafsplugin.fullname" . }}']
  restartPolicy: Never
